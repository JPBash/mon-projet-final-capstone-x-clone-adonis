<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profil de {{ user.fullName }}</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <main class="app-shell">
        <div class="layout">
            <aside class="sidebar">
                <div class="brand">X</div>
                <nav class="nav-menu">
                    <a href="{{ route('home') }}" class="nav-link">Accueil</a>
                    <a href="{{ route('profile.show') }}" class="nav-link active">Profil</a>
                </nav>
                <div class="user-chip">
                  <div class="avatar">
                    @if(user.avatarUrl)
                      <img src="{{ user.avatarUrl }}" style="width:100%; height:100%; border-radius:50%; object-fit:cover;">
                    @else
                      {{ user.fullName.charAt(0) }}
                    @endif
                  </div>
                  <div>
                    <p><strong>{{ user.fullName }}</strong></p>
                    <p class="muted">{{ user.email }}</p>
                  </div>
                </div>
            </aside>

            <section class="profile-page">
                <form action="{{ route('profile.update') }}" method="POST" enctype="multipart/form-data">
                    {{ csrfField() }}
                    
                    {{-- Couverture --}}
                    <div class="profile-cover" 
                         style="background-image: url('{{ user.coverUrl || '/default-cover.jpg' }}'); background-size: cover; background-position: center; height: 200px; background-color: #333;">
                    </div>
                    
                    <div class="profile-head" style="padding: 0 1rem;">
                        <div class="profile-avatar-wrap">
                            <div class="avatar avatar-xl" 
                                 style="background-image: url('{{ user.avatarUrl || '' }}'); background-size: cover; background-position: center; width: 120px; height: 120px; border: 4px solid black; margin-top: -60px; background-color: #1da1f2; display: flex; align-items: center; justify-content: center; position: relative; overflow: hidden;">
                                @if(!user.avatarUrl)
                                    {{ user.fullName.charAt(0) }}
                                @endif
                            </div>
                        </div>

                        <div style="margin-top: 15px;">
                            <h2>{{ user.fullName }}</h2>
                            <p class="muted">{{ user.email }}</p>
                        </div>

                        <div style="margin-top: 20px; display: flex; flex-direction: column; gap: 10px; padding: 10px; background: #16181c; border-radius: 10px;">
                            <label style="font-size: 0.8rem; color: gray;">Modifier les photos :</label>
                            <input type="file" name="avatar" accept="image/*" style="font-size: 0.8rem;">
                            <input type="file" name="cover" accept="image/*" style="font-size: 0.8rem;">
                            <button type="submit" class="btn btn-primary" style="width: fit-content; padding: 5px 15px;">Enregistrer</button>
                        </div>
                    </div>
                </form>

                <div class="stats" style="padding: 1rem; display: flex; gap: 20px;">
                    <span><strong>0</strong> Abonnements</span>
                    <span><strong>0</strong> Abonnés</span>
                </div>

                <hr style="border: 0.5px solid #333; margin: 0;">
                
                <h3 style="padding: 1rem;">Mes Posts</h3>
                <div id="profile-tweets" class="tweet-list">
                    @each(tweet in tweets)
                        <div class="tweet" style="padding: 1rem; border-bottom: 1px solid #333; display: flex; gap: 1rem;">
                            <div class="avatar">{{ user.fullName.charAt(0) }}</div>
                            <div>
                                <strong>{{ user.fullName }}</strong>
                                <p>{{ tweet.content }}</p>
                            </div>
                        </div>
                    @else
                        <p style="padding: 2rem; text-align: center;" class="muted">Vous n'avez pas encore posté.</p>
                    @endeach
                </div>
            </section>
        </div>
    </main>
</body>
</html>