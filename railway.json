{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "node server.js",
    "releaseCommand": "node ace migration:run --force",
    "restartPolicyType": "ON_FAILURE"
  }
}
